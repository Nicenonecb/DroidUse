# DroidUse 项目协作约定

## 文件规模与模块组织

- 人工维护的源码、测试、脚本、配置、文档和补丁文件，单文件最多 **1500 行**（含空行和注释，以 `wc -l` 为准）。生成文件和第三方原样引入的文件不作为拆分对象。
- 新增文件应直接遵守上限；文件接近上限时主动拆分。修改已有超限文件时，先按职责拆分相关内容，再完成本次改动；不要继续向超限文件追加内容，也不要靠删除空行、压缩格式或堆叠多个职责来凑行数。
- 拆分应形成清晰的模块边界：按功能或职责归档，把实现、接口、数据模型、平台适配和测试放在对应位置；让入口文件负责组装与协调，避免循环依赖和无意义的单函数文件。同步更新导入、构建配置、补丁应用顺序及文档引用，确保行为保持一致。
- 完成改动前检查本次新增和修改的人工维护文件行数，确认均不超过 1500 行；运行与拆分范围相称的构建或测试，报告尚未验证的部分。

## 长时间任务：启动后交接

门槛为单项任务预计耗时 **15 分钟及以上（≥15 min）**，包括编译、下载、安装、打包及服务器任务。达到门槛时，启动确认后立即交接，不先等满 15 分钟。预计不足 15 分钟可正常继续；若执行中发现预计总耗时达到门槛，或实际已运行 15 分钟仍未完成，则在安全位置交接。交接是停止助手轮询并结束本轮回复，后台任务继续运行。刷 ROM 另按下方规则处理。

1. 完成必要准备后，直接启动已经获得用户授权的任务。服务器任务使用断开连接后仍可运行的方式（例如 `nohup` 或 `tmux`），保存日志、退出码和开始时间；每次运行使用独立记录，避免把旧结果当成本轮结果。
2. 只做一次简短的启动确认，检查命令确实已启动或已快速失败。确认启动后立即结束本轮回复，把任务留在后台运行；停止定时轮询、重复等待和无变化的进度播报。等待用户说“继续”或主动询问进度，再检查。
3. 交接回复必须包含以下信息，提供本次实际路径和可直接复制的命令：
   - **已启动什么**：任务、执行机器、工作目录、实际执行命令，以及 PID、tmux 会话名或其他任务标识。
   - **预计多久**：开始时间、预计耗时范围、建议再次查看的时间（北京时间）；依据历史构建耗时或当前工具估算。没有依据时明确说明估时不确定，不保证某个时间一定完成。
   - **如何查看进度**：实际日志位置和查看命令，例如 `tail -n 30 <日志路径>`；远程任务给出所需的 SSH 连接方式。需要实时查看时可附 `tail -f`，说明 Ctrl+C 只退出日志查看。
   - **什么代表完成**：本轮退出码为 0、对应成功日志及预期产物路径。明确非零退出码表示失败；没有退出码记录时结合进程状态判断，不能只凭“日志停止增长”认定成功。
   - **下一步**：完成后需要的校验、安装或测试，以及用户回复“继续”后会执行什么。将“命令执行成功”和“产物校验/实机验收完成”分开说明。
   - **需要保持什么**：按实际任务说明服务器需保持开机、手机是否需连接 USB 等条件；可脱离 Mac 运行的服务器任务应明确告知。
4. 用户回复“继续”后，先读取已有任务的状态和退出码，再处理结果；复用正在运行的任务，避免重复启动。仍在运行时简短报告更新后的估时并再次结束本轮。
5. 只有用户明确要求持续监控或自动通知时，才改变上述交接方式；默认不创建定时检查或自动续跑任务。

## 刷 ROM 的暂停位置

刷 ROM 前先完成包校验、设备识别和必要备份，然后在实际写入前停下，交代步骤、预计耗时和设备连接要求，等待用户明确继续刷机。写入一旦开始，应等待当前步骤安全结束再停；不得为结束聊天而强行中断正在写入的操作。
